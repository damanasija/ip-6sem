<% include ./partials/header%>
<%include ./navbar %>

<style>
  div#message{
    display: none;
  }
</style>

<h1 class="jumbotron text-center">Login</h1>

<div class="container center-block">
  <div class="container alert alert-danger center-block text-center" style="display:<%= display %>">
    <h4><strong><%= error %></strong></h4>
  </div>
  <form method="POST" action="/login" onsubmit="return validateForm()">


    <div class="center-block">
      <!--Field for username -->
      <div class="col-md-3"></div>

      <div class="col-md-6">
        <h4 class="lead text-center bg-info" style="width: 25%;">Enter Details</h4>
        <div class="form-group">

          <label for="email">Email</label>
          <input type="text" name="email" class="form-control" id="email" placeholder="Email" required>
        </div>

        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" name="password" class="form-control" id="password" placeholder="Password" required>
        </div>
        <input type="submit" class="btn btn-success" value="Submit">
        <div id="message" class="alert alert-danger text-center"></div>
      </div>
    </div>

    <div class="col-md-3"></div>
  </form>
</div>
<!-- <script>
  const displayError = (message) => {
    let messageDiv = document.getElementById("message");
    messageDiv.innerHTML = message;
    messageDiv.style.display = "block";
    setTimeout(() => {
      messageDiv.style.display = "none";      
    }, 2000);
  }
  function validateForm () {
    let messageDiv = document.getElementById("message");
    let username = document.getElementById("email");
    let password = document.getElementById("password");
    if((username.value == "") && (password.value == "")){
      displayError("<strong>All fields are required.</strong>");
      return false;
    }
    if (username.value == "") {
      displayError("Username is empty.")
      return false;
    }
    if (username.value.length < 4) {
      displayError("<strong>Username</strong> is short!");
      return false;
    }
    if (password.value == "") {
      displayError("<strong>Password</strong> field is empty!");
      return false;
    }
    return true;
  }
</script> -->


<% include ./partials/footer%>